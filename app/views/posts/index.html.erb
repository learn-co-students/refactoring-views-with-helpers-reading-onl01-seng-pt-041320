<% @posts.each do |post| %>
  <div><%= post.title %> - <%= last_updated post %></div>
  <!--<div><%= post.title %> - <%= post.updated_at.strftime("Last updated %A, %b %e, at %1:%M %p") %></div>-->
<% end %>